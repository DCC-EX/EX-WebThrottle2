export enum STORAGE_KEYS {
  SAVED_CONNECTIONS = 'exwebthrottle_connections',
  THEME = 'exwebthrottle_theme',
  CONTROLLER = 'exwebthrottle_controller',
}

export enum ConnectionMethods {
  Serial = 'Serial',
  Emulator = 'Emulator',
}
