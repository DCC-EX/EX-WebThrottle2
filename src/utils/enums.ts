export enum STORAGE_KEYS {
    SAVED_CONNECTIONS = "webthrottleex_connections",
    THEME = "webthrottleex_theme",
}

export enum ConnectionMethods {
    Serial = 'Serial',
    Emulator = 'Emulator',
}
